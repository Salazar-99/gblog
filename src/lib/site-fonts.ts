import { GeistMono } from "geist/font/mono";
import { GeistSans } from "geist/font/sans";

/** Same Geist Sans stack as blog prose (`--font-sans` / `font-sans`). */
export { GeistMono, GeistSans };

export const siteSansFamily = "Geist Sans";
